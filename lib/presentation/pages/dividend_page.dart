import 'package:flutter/material.dart';

class DividendPage extends StatelessWidget {
  const DividendPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dividend Page Content'),
    );
  }
}
